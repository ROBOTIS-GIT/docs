name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: docs_ci
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./docusaurus
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: docusaurus/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Build English site
        env:
          DOCUSAURUS_IGNORE_SSG_WARNINGS: 'true'
          DOCUSAURUS_URL: 'https://docs.robotis.com'
          DOCUSAURUS_BASE_URL: '/'
          DOCUSAURUS_ON_BROKEN_LINKS: 'warn'
          NODE_OPTIONS: --max_old_space_size=4096
        run: npm run build -- --locale en

      - name: Build Korean site
        env:
          DOCUSAURUS_IGNORE_SSG_WARNINGS: 'true'
          DOCUSAURUS_URL: 'https://docs.robotis.com'
          DOCUSAURUS_BASE_URL: '/ko/'
          DOCUSAURUS_ON_BROKEN_LINKS: 'warn'
          NODE_OPTIONS: --max_old_space_size=4096
        run: npm run build -- --locale ko --out-dir build/ko

      - name: Check build output
        working-directory: ./
        run: |
          test -d docusaurus/build
          test -f docusaurus/build/index.html
          test -d docusaurus/build/ko
          test -f docusaurus/build/ko/index.html

      - name: Upload build artifacts on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: build-artifacts-${{ github.sha }}
          path: |
            docusaurus/build
            docusaurus/.docusaurus
          retention-days: 7
