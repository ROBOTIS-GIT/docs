# ROBOTIS emanual: Jekyll → Docusaurus 마이그레이션 플랜

## 1. 디렉터리 구조

```
c:/Data/emanual/
├── source/             # ROBOTIS-GIT/emanual 원본 Jekyll (clone, 읽기 전용)
├── docusaurus/         # 신규 Docusaurus 프로젝트 (작업 대상)
├── scripts/            # 변환/검증 자동화 스크립트
├── .claude/agents/     # 5개 sub-agent 정의
└── MIGRATION.md
```

## 2. 핵심 사실 (analyzer 보고 기반)

| 항목 | 값 |
|---|---|
| 원본 파일 수 | 13,723개 (clone 기준) |
| 제품 폴더 (docs/en/) | 8개 — dxl, dyd, edu, parts, platform, software, faq, popup |
| 언어 | en, kr, jp (모두 유지 → Docusaurus locale: en/ko/ja) |
| 양언어 페어링 키 | frontmatter `ref:` |
| Liquid `{% include %}` 호출 | ~9,796건 |
| 재사용 fragment 파일 | ~146개 (특히 `_includes/en/dxl/`) |
| 자산 크기 | 1.0 GB (대부분 `assets/images/`) |
| 검색 인덱서 | 커스텀 C 바이너리 — Docusaurus 검색 플러그인으로 대체 |
| 사이드바 정의 | 단일 `_data/navigation.yml` (~800줄, 양언어 키 혼재) |
| frontmatter 특이 필드 | `tabs/tab_titleN`, `sidebar.nav`, `permalink`, `product_group`, `page_number` |

## 3. 5개 에이전트 팀 (`.claude/agents/`)

| 에이전트 | 한 줄 역할 | 모델 |
|---|---|---|
| [analyzer](.claude/agents/analyzer.md) | 원본 인벤토리/패턴/통계 산출 (읽기 전용) | sonnet |
| [content-migrator](.claude/agents/content-migrator.md) | .md → .mdx 변환 (frontmatter, kramdown, Liquid include, 이미지 경로) | sonnet |
| [structure-architect](.claude/agents/structure-architect.md) | docusaurus.config.js, sidebars.js, redirect, 커스텀 컴포넌트, 테마 | opus |
| [i18n-translator](.claude/agents/i18n-translator.md) | en↔ko 페어링, i18n 디렉터리 정합성, UI 번역 키 | sonnet |
| [qa-reviewer](.claude/agents/qa-reviewer.md) | 빌드/링크/잔여 Liquid/페어링/리다이렉트 검증 (수정 안 함) | sonnet |

## 4. 단계별 진행

### Phase 0 — 준비 (현재 단계)
- [x] 원본 clone (`source/`)
- [x] 구조 분석 보고 (analyzer 1차)
- [x] 5개 에이전트 정의
- [ ] Docusaurus 프로젝트 스캐폴딩 (`docusaurus/`)
- [ ] redirect 자동 생성 스크립트 골격 (`scripts/build-redirects.js`)

### Phase 1 — 골격
- structure-architect: `docusaurus.config.js` 초안 (preset classic, i18n en/ko, search)
- structure-architect: `sidebars.js` 초안 (8개 제품 그룹 수동 + 그 안은 autogenerated)
- structure-architect: 테마 색상/레이아웃 1차 (Dyte 스타일 참고)
- 1개 시범 제품 (예: `dxl/ax`) 으로 end-to-end 파이프라인 검증

### Phase 2 — 변환 자동화
- 공통 fragment(`_includes/en/dxl/control_table_*` 등) 14~146개를 partial/컴포넌트로 1차 추출 (structure-architect + content-migrator 협업)
- content-migrator로 제품군 단위 일괄 변환:
  1. `dxl/` (가장 큼, control table 패턴 활용)
  2. `dyd/`
  3. `platform/`
  4. `software/`
  5. `parts/`, `edu/`, `faq/`, `popup/`
- 각 제품군 변환 직후 qa-reviewer로 검증 → action item 라우팅 → 재변환

### Phase 3 — 다국어
- i18n-translator로 ko 디렉터리 미러 구성
- navbar/footer 한국어 텍스트 추출 → `i18n/ko/docusaurus-theme-classic/*.json`
- 누락 ko 페이지 목록 산출 (자동 번역 금지)

### Phase 4 — 자산 & 검색
- `source/assets/images/` → `docusaurus/static/img/` 복사 (구조 보존)
- 검색 플러그인 통합 (`@easyops-cn/docusaurus-search-local` 또는 Algolia)
- 양언어 검색 인덱싱 검증

### Phase 5 — 리다이렉트 & 출시 준비
- 원본 `permalink:` 전부 추출 → `redirects` 배열로 자동 생성
- qa-reviewer 최종 통과 (빌드 + 깨진 링크 0)
- 미해결 이슈 백로그 정리

## 5. 확정 사항

- **언어**: en (default) + ko + ja (원본 jp/ → Docusaurus ja locale)
- **검색**: `@easyops-cn/docusaurus-search-local` (다국어 인덱스, 오프라인 빌드 가능)
- **호스팅**: GitHub repository 기반 (Luke-K-robotis 계정, repo 이름 미정). `docs.robotis.com` 도메인 보존 가능성 있음 — repo 결정 시 확인.
- **Redirect**: `@docusaurus/plugin-client-redirects` (빌드 시 정적 HTML redirect 페이지 생성). GitHub Pages 환경에서도 동작. `scripts/build-redirects.js` 가 원본 `permalink:` 전부를 추출해 `redirects.generated.json` 으로 출력 → config가 import.
- **`tabs:` 시스템**: 단일 `<Tabs>`/`<TabItem>` (Docusaurus 기본 `@theme/Tabs`) 으로 통합. 변형(Revision, ROS 버전, OS 버전 등)은 모두 같은 컴포넌트 인터페이스, content-migrator가 본문 변환 시 import 추가.
- **`product_group`**: frontmatter `tags: [<product_group>]` 으로 보존. Docusaurus 기본 태그 페이지(`/docs/tags/<group>`)가 자동 생성됨. 별도 카탈로그/필터 UI는 추후 필요시.
- **Git**: `c:/Data/emanual/` 자체를 git repo로 사용. 원본 `source/` 디렉터리는 `.gitignore` 처리(별도 .git, 1GB 자산). `showLastUpdateTime: true` 활성화.

## 6. 미정 항목 (작업 시 확인 필요)

- Luke-K-robotis 계정의 신규 repo 이름 (예: `emanual-docusaurus`, `emanual-v2`)
- `docs.robotis.com` 도메인을 신규 repo로 그대로 옮길지 (CNAME 이전)
- Git commit 작성자 — 글로벌은 `gooddeng/gooddeng@naver.com`. 이 repo만 local config로 Luke-K-robotis 계정으로 변경할지

## 7. 호출 패턴 예시

- `Use the analyzer agent to inventory all frontmatter fields used in source/docs/en/dxl/`
- `Use the content-migrator agent to convert source/docs/en/dxl/ax/ to docusaurus/docs/dxl/ax/`
- `Use the structure-architect agent to draft sidebars.js for the dxl product group`
- `Use the i18n-translator agent to mirror docs/en/dxl/ax/ to ko locale using ref pairing`
- `Use the qa-reviewer agent to verify the dxl/ax migration`
