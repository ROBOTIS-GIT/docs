# Front-end & Asset Optimization Guide

ROBOTIS e-Manual (Docusaurus 기반) 프론트엔드 UI 구성과 자산/빌드 사이즈 최적화의 운영 가이드. 다른 부서/담당자가 작업을 이어받을 때 참고할 자료.

- 사이트: https://docs.robotis.com
- Repo: https://github.com/Luke-K-Robotis/emanual-v2 (master)
- Docusaurus: 3.10.x (classic preset)
- Node: 20+
- Locale: en (기본), ko (ja 는 v1 직전 삭제)
- 1차 완료 태그: `v1.0-phase1` (이관 시점 스냅샷)

---

## 1. 디렉터리 한눈에

```
c:/Data/emanual/
├── source/                                # 원본 Jekyll repo clone (read-only 참고)
├── docusaurus/                            # 현재 사이트 코드
│   ├── docs/                              # 영문(default locale) 콘텐츠
│   ├── i18n/ko/                           # 한국어 번역 (UI + 콘텐츠)
│   ├── src/
│   │   ├── pages/
│   │   │   ├── index.tsx                  # 홈페이지 React 컴포넌트
│   │   │   └── index.module.css           # 홈페이지 CSS 모듈
│   │   ├── css/custom.css                 # 전역 디자인 토큰 + navbar/mega-menu
│   │   └── clients/mega-menu-sticky.js    # mega-menu 활성화 JS (sticky)
│   ├── static/img/                        # 이미지 자산 (WebP 우선)
│   │   ├── home/                          # 홈 카드 4장
│   │   ├── mega-menu/                     # navbar mega-menu 썸네일 21장
│   │   ├── emanaul_logo.webp              # navbar 로고 (sic, 파일명 유지)
│   │   ├── favicon.ico                    # ROBOTIS 공식 favicon
│   │   └── dxl/, platform/, software/...  # 콘텐츠 자산
│   ├── docusaurus.config.ts               # 사이트 설정 (navbar/footer/플러그인)
│   ├── sidebars.ts                        # 사이드바 트리 (hand-curated)
│   └── redirects.generated.json           # 자동 생성된 redirect 매핑
├── scripts/                               # 운영 스크립트 (Node)
├── .github/workflows/deploy.yml           # GitHub Actions 배포 워크플로
└── FRONTEND.md                            # 이 문서
```

---

## 2. 프론트엔드 디자인 시스템

### 2.1 색상 토큰 (`src/css/custom.css`)

ROBOTIS Blue 계열. light/dark 둘 다 정의.

| 변수 | Light | Dark |
|---|---|---|
| `--ifm-color-primary` | `#2160fd` | `#4b7cfd` |
| `--ifm-color-primary-dark` | `#1145e0` | `#2160fd` |
| `--ifm-color-primary-darker` | `#0e35b8` | `#1145e0` |
| `--ifm-color-primary-darkest` | `#0e2c8d` | `#0e35b8` |
| `--ifm-color-primary-light` | `#4b7cfd` | `#7da4fe` |
| `--ifm-color-primary-lighter` | `#7da4fe` | `#adc8ff` |
| `--ifm-color-primary-lightest` | `#adc8ff` | `#d6e3ff` |
| `--robotis-muted` | `#5b6573` | `#9aa4b1` |

primary 색을 변경할 때는 위 7개를 함께 갱신해야 hover/active 등의 단계가 자연스럽다. shadow에 들어가는 `rgba(33, 96, 253, X)` 도 같이 손볼 것 (`index.module.css` hero/CTA, `--robotis-card-shadow-hover`).

### 2.2 폰트
- Pretendard variable font CDN (`jsdelivr`) — `custom.css` 상단 `@import`
- fallback: Apple SD Gothic Neo / Noto Sans KR / Malgun Gothic / system

### 2.3 홈페이지 (`src/pages/index.tsx` + `index.module.css`)

세 섹션:

1. **HomeHero** — badge + h1 title + subtitle + 2 CTA. 배경은 grid pattern + radial glow (light/dark 분기).
2. **ProductGrid** — 4개 image card (DYNAMIXEL/Platform/Software/Parts). hover 시 translateY + image scale + top gradient line.
3. **PopularGuides** — quick link row 6개. background `.sectionAlt` 는 현재 transparent.

i18n 키는 모두 `<Translate id="home.…">` 로 정의. 새 텍스트 추가 후 `npx docusaurus write-translations --locale ko` 로 키 추출.

### 2.4 Navbar

`docusaurus.config.ts` 의 `themeConfig.navbar.items`:

```
[로고 emanaul_logo.webp 150px]   DYNAMIXEL ▾   Systems ▾   Software   Parts   Edu   FAQ           [한국어 ▾]
```

- 로고: `static/img/emanaul_logo.webp`. width 150px 고정 (`custom.css`).
- Navbar 우측 아이콘: locale globe / GitHub external-link 화살표를 CSS로 숨김 (`custom.css` 내 `.navbar svg[class*='iconLanguage']` 등).
- DYNAMIXEL · Systems 메뉴 두 개가 **mega-menu** (커스텀 dropdown). 자세히는 §3.

### 2.5 Sidebar (`sidebars.ts`)

Hand-curated + 폴더 단위 autogenerated 의 혼합. 원본 `_data/navigation.yml` 순서 보존 + 카테고리 link 명시.

#### DYNAMIXEL 영역 (3 sidebar)

| sidebar id | 페이지 | 비고 |
|---|---|---|
| `dxlQuickStartSidebar` | dxl/index, dxl-quick-start-guide, dxl-quick-start-insert | DYNAMIXEL 메뉴 → Quick Start 카테고리 |
| `dxlSidebar` | 9 시리즈 카테고리 (Y/P/X/MX/AX/EX/DX/RX/PRO) + 각 시리즈 인덱스 + 모델 페이지 | DYNAMIXEL 메뉴 → DYNAMIXEL 카테고리. Y/P/X/PRO 인덱스 페이지의 `sidebar_label`을 모두 "Overview"(ko: "개요")로 정규화 + `sidebar_position:1` 로 카테고리 맨 위 고정 |
| `protocolSidebar` | protocol1, protocol2, crc | DYNAMIXEL 메뉴 → Protocol 카테고리 |

#### Systems(=platform) 영역 (15 sidebar)

mega-menu 의 각 제품 링크가 진입할 때 그 **제품 전용 사이드바**가 표시되도록 폴더 단위로 분할. `sidebars.ts` 객체 키 순서가 곧 검색 우선순위라 좁은 범위(제품 폴더) 사이드바를 `platformSidebar` 보다 먼저 둔다.

| sidebar id | dirName | 메가메뉴 진입 페이지 |
|---|---|---|
| `aiworkerSidebar` | platform/aiworker | /docs/platform/aiworker |
| `aisapiensSidebar` | platform/aisapiens | /docs/platform/aisapiens |
| `opSidebar` | platform/op | /docs/platform/op/getting_started |
| `op2Sidebar` | platform/op2 | (메가메뉴 미노출, 사이드바만) |
| `op3Sidebar` | platform/op3 | (메가메뉴 미노출, 사이드바만) |
| `thormang3Sidebar` | platform/thormang3 | /docs/platform/thormang3/introduction |
| `hx5d20Sidebar` | platform/hx5_d20 | /docs/platform/hx5_d20 |
| `rhp12rnSidebar` | platform/rh_p12_rn | /docs/platform/rh_p12_rn |
| `omySidebar` | platform/omy | /docs/platform/omy/overview |
| `omxSidebar` | platform/omx | /docs/platform/omx |
| `openmanipulatorPSidebar` | platform/openmanipulator_p | /docs/platform/openmanipulator_p/overview |
| `openmanipulatorXSidebar` | platform/openmanipulator_x | /docs/platform/openmanipulator_x/overview |
| `manipulatorHSidebar` | platform/manipulator_h | /docs/platform/manipulator_h/introduction |
| `turtlebot3Sidebar` | platform/turtlebot3 (category 래퍼) | /docs/category/turtlebot3 (generated-index) |
| `platformSidebar` | platform 단일 페이지 + common/msgs | mega-menu 외 잡탕용 fallback |

#### 그 외 (4 sidebar)

| sidebar id | 페이지 | 비고 |
|---|---|---|
| `softwareSidebar` | autogenerated `software/` | software/index.mdx 가 `sidebar_label:"Overview"`(ko:"개요") + `sidebar_position:0` + `slug:/software` → Software 대메뉴 클릭 시 첫 화면 |
| `partsSidebar` | autogenerated `parts/` | |
| `eduSidebar` | autogenerated `edu/` | |
| `faqSidebar` | autogenerated `faq/` | |

#### Doc id 규칙 (필수 인지)

페이지의 frontmatter `id`가 명시되면 doc id가 `<dir>/<id>` 형태가 된다. 예: `dxl/x/index.mdx` 의 `id: x` → doc id = `dxl/x/x`. 사이드바에서 `link.type: 'doc'` 으로 매칭할 때 이 점에 주의.

> 단, **URL** 은 frontmatter id 와 무관하게 `index.mdx` 의 경우 폴더 URL(`/docs/dxl/x`)로 매핑된다. mega-menu href 는 doc id 가 아닌 **실제 URL** 을 써야 한다 (예: `/docs/dxl/x/x` 는 404, `/docs/dxl/x` 가 정답).

### 2.6 Mega-menu (DYNAMIXEL, Systems)

순수 React 컴포넌트가 아닌 **`type: 'html'` 인라인 HTML** + **vanilla JS sticky** 패턴.

- HTML 구조는 `docusaurus.config.ts` 의 `items: [{type: 'html', value: '<div class="mega-menu">…</div>'}]` 안에.
- DYNAMIXEL: 좌측 카테고리 3개 (Quick Start / DYNAMIXEL / Protocol).
- Systems: 좌측 카테고리 4개 (Humanoid / Robot Hand / Manipulator / **TurtleBot3**). TurtleBot3 패널은 단일 항목이며 `/docs/category/turtlebot3` (generated-index) 으로 연결.
- 활성화 동작: `src/clients/mega-menu-sticky.js`
  - 카테고리 `mouseenter` / `focus` 시 `--active` 클래스 토글.
  - `mouseleave` 핸들러 없음 → 한 번 활성화된 panel 은 다른 카테고리로 가기 전까지 sticky. 마우스가 우측 영역으로 이동해도 panel 표시 유지.
  - `MutationObserver` 로 SPA 라우팅 후 새 mega-menu DOM 도 자동 init.
- CSS는 `custom.css` 의 `.mega-menu*` 클래스. `:has()` 도 정의되어 있지만 fallback 용. 실제 활성/비활성은 JS가 부여하는 `--active` 클래스로 제어.

> **mega-menu href 작성 규칙**: 반드시 브라우저가 실제로 가는 URL을 사용. `index.mdx`(또는 frontmatter id ≠ 폴더명) 인 페이지를 doc-id 형식 (`/docs/dxl/x/x`) 으로 잘못 적으면 404. 폴더 URL(`/docs/dxl/x`)이 정답. PR 검토 시 mega-menu URL 변경분은 로컬 빌드 후 `build/docs/...` 에 실제 파일 생성 여부로 검증할 것.

#### 썸네일 (`static/img/mega-menu/`)

총 21장 + Photoshop 원본 (`썸네일.psd`) 보관.

| 분류 | 파일 | 표시 크기 | 권장 소스 크기 |
|---|---|---|---|
| DYNAMIXEL 시리즈 9개 | `Y-series.webp`, `p-series.webp`, `x-series.webp`, `mx-series.webp`, `ax-series.webp`, `ex-series.webp`, `dx-series.webp`, `rx-series.webp`, `pro-series.webp` | 72×72 (3-col grid) | 200×200 정사각, 투명 배경 |
| Humanoid 4개 | `ai-worker.webp`, `ai-sapiens.webp`, `robotos-op.webp` (오타이지만 의도 표기), `thormang3.webp` | 72×72 (2-col grid) | 동일 |
| Robot Hand 2개 | `hx5-d20.webp`, `rh-p12-rn.webp` (RH-P12-RN-UR 도 같은 파일 공유) | 72×72 (2-col grid) | 동일 |
| AI Manipulator 2개 | `omy.webp`, `omx.webp` | 72×72 (2-col grid) | 동일 |
| OpenManipulator 3개 | `openmanipulator-p.webp`, `openmanipulator-x.webp`, `manipulator-h.webp` | 36×36 (row list) | 동일 |
| TurtleBot3 1개 | `turtlebot3.webp` | 72×72 (1-col grid) | 동일 |

추가/교체 시 `docusaurus.config.ts` 의 `<img src="/img/mega-menu/<name>.webp" alt="…" />` 부분 수정. CSS는 자동으로 `object-fit: contain` 적용 (border-radius / 원형 마스크는 제거됨). 파일명 대소문자는 Linux CI에서 case-sensitive 하므로 정확히 맞출 것.

---

## 3. 자산 최적화 워크플로 (스크립트 카탈로그)

`scripts/` 안의 운영 도구. 모두 Node 20+ + Docusaurus 디렉터리 기준. 카테고리별:

- **변환 (1회성, 보존용)**: `convert-*.js` (16개) — 마이그레이션 당시 Jekyll md → Docusaurus mdx 일괄 변환에 쓰임. 새 콘텐츠에는 사용하지 않지만 향후 추가 마이그레이션 발생 시 패턴 참고용으로 보존.
- **자산 압축 (반복 운영)**: `png-to-webp.js`, `jpg-to-webp.js`, `rewrite-png-references.js`, `rewrite-jpg-references.js`, `measure-png-compression.js`
- **redirect**: `build-redirects.js`
- **헤딩 anchor**: `inject-heading-anchors.js`, `inject-anchors-rna.js`, `fix-heading-anchors.js`, `hide-popup-sidebar.js`
- **링크/자산 복원**: `restore-internal-links.js`, `recover-missing-images.js`

### 3.1 이미지 압축

이미지 추가/갱신 후 두 단계로 진행.

```bash
# 1) PNG → WebP q=85 (alpha 보존)
node scripts/png-to-webp.js

# 2) JPG → WebP q=85
node scripts/jpg-to-webp.js

# 3) mdx 및 소스 파일의 image src 확장자 일괄 치환
node scripts/rewrite-png-references.js
node scripts/rewrite-jpg-references.js
```

> 두 rewrite 스크립트는 **실제 .webp 파일이 `static/img/` 에 존재할 때만** 경로를 교체한다. 따라서 변환 직후 실행하면 안전.

**압축률 사전 측정**: `node scripts/measure-png-compression.js` — 200개 샘플로 JPEG q=80/85, WebP lossless/q=80/q=85 변환 비율을 추정.

| 형식 | PNG 대비 비율 (q=85, 측정값) | 비고 |
|---|---|---|
| JPEG q=85 | 14.2% | alpha 손실 |
| WebP lossless | 44.7% | alpha 보존 무손실 |
| WebP q=85 ✅ | 10.3% | alpha 보존 + 안전 화질, **권장** |

실측 효과: PNG 630 MB → 147 MB (77% 절약), JPG 192 MB → 62 MB (68% 절약).

### 3.2 redirect 자동 생성 (`scripts/build-redirects.js`)

원본 Jekyll 의 모든 `permalink:` 값을 스캔해 Docusaurus client-redirects 가 사용할 `redirects.generated.json` 생성.

- target 페이지가 `docusaurus/docs/` 에 실제 존재할 때만 emit (점진적 활성화).
- en locale 만 emit (ko/ja path 는 plugin 검증 한계로 제외).
- `PERMALINK_OVERRIDES` 로 RH-P12-RN 같이 특수 매핑 처리.

콘텐츠가 추가될 때마다 다시 실행해야 새 redirect 가 생성된다.

### 3.4 헤딩 anchor 처리

- `scripts/inject-heading-anchors.js <series>` — 원본 `<a name="X">` 정보를 변환된 mdx 헤딩 직전에 `<a id="X"></a>` 로 부착 (idempotent).
- `scripts/fix-heading-anchors.js` — 헤딩 line 안의 markdown self-link `[Foo](#foo)` / inline `<a name>` 제거. nested `<a>` SSG warning 방지.

### 3.5 내부 링크 복원

`scripts/restore-internal-links.js` — 변환 중 미완 영역 보호용으로 `https://emanual.robotis.com/docs/...` 외부 URL 로 강등됐던 링크들을 `/docs/...` 내부 링크로 복원 (target 페이지가 docs 트리에 실제 존재할 때만).

### 3.6 누락 자산 복원

`scripts/recover-missing-images.js` — 모든 mdx 의 `/img/*` 참조를 스캔, 누락된 파일은 `source/assets/images/` 에서 (대소문자 무관) 찾아 복사. backslash path 정규화도 같이.

---

## 4. 빌드 사이즈 한계 관리

GitHub Pages artifact 한계는 **1 GB**. 운영 중 빌드 결과 사이즈를 항상 1 GB 안에 유지해야 한다.

### 4.1 현재 마진

```
빌드 단계별 사이즈 (raw)
3.7 GB (원본 PNG/JPG)
↓ PNG → WebP q=85
1.54 GB
↓ JPG → WebP q=85
1.345 GB
↓ ja locale 제거 (현재 상태)
0.949 GB ✅
```

GitHub Actions 의 artifact 업로드는 gzip 압축되므로 **실제 업로드 사이즈는 약 500–600 MB** 수준이 된다.

### 4.2 새 자산 추가 시 체크리스트

1. 가능하면 **WebP** 로 저장 (alpha 있으면 q=85, 없으면 q=80).
2. 큰 이미지는 200–800 KB 이내로 압축.
3. `npm run build` 결과 `du -sh docusaurus/build` 1 GB 이내인지 확인.
4. 1 GB 근처면 호스팅 변경(Cloudflare Pages 25 GB) 검토.

### 4.3 1 GB 초과 시 대안

| 방법 | 효과 | 비고 |
|---|---|---|
| 추가 이미지 압축 | 작음 (이미 WebP) | `scripts/jpg-to-webp.js`, sharp 옵션 q=75 |
| 새 locale 추가 보류 | locale 1개당 약 400 MB | webpack chunks가 locale별 별도 |
| 외부 CDN (이미지 분리) | 큼 (500 MB+) | `static/img/` 를 Cloudflare R2 등으로 |
| Cloudflare Pages 이전 | 한계 25 GB | DNS만 변경, repo/도메인 유지 |

---

## 5. 운영 자주 묻는 작업

### 5.1 새 페이지 추가

1. `docusaurus/docs/<area>/<name>.mdx` 작성 (frontmatter `id`, `title`, `sidebar_label`).
2. 같은 path 의 ko 미러: `docusaurus/i18n/ko/docusaurus-plugin-content-docs/current/<area>/<name>.mdx` (없으면 영문 fallback).
3. `sidebars.ts` 의 해당 sidebar 트리에 등록 (autogenerated 면 자동).
4. `node scripts/build-redirects.js` 로 redirect 갱신.
5. `npm run build` 로 검증.

### 5.2 새 navbar mega-menu 항목 추가

1. 썸네일을 `static/img/mega-menu/<name>.webp` (200×200 정사각, 투명).
2. `docusaurus.config.ts` 의 해당 mega-menu HTML 에 `<a class="mega-menu__product"…><img src="/img/mega-menu/<name>.webp" …/><span>이름</span></a>` 추가.
3. **href는 실제 페이지 URL로**. `index.mdx` 인 페이지면 `/docs/<폴더>/` (frontmatter id 가 폴더명과 달라도 URL 은 폴더 그대로). 카테고리 인덱스(generated-index)면 `/docs/category/<카테고리-slug>` (slug 는 라벨에서 자동 lowercase + space→hyphen).
4. 새 카테고리 인덱스 페이지가 필요하면 `sidebars.ts` 에 `{type:'category', label:'…', link:{type:'generated-index'}, items:[…]}` 형태로 사이드바 루트를 감싸야 한다 (예: `turtlebot3Sidebar`).
5. 새 제품 폴더면 `sidebars.ts` 에 `{type:'autogenerated', dirName:'platform/<name>'}` 사이드바 1줄 추가 — 그래야 진입 시 platformSidebar(공통) 대신 제품 전용 사이드바가 뜬다.
6. 로컬 빌드 후 `build/docs/<해당-경로>/index.html` 생성 여부로 404 검증.

### 5.3 사이드바 카테고리 라벨 한국어화

`i18n/ko/docusaurus-plugin-content-docs/current.json` 의 `sidebar.<sidebarId>.category.<Label>` 키. 자동 추출은:

```bash
cd docusaurus
npx docusaurus write-translations --locale ko
```

### 5.4 i18n 텍스트 추가

소스에서 `<Translate id="…">텍스트</Translate>` 사용 후 `write-translations` 실행. ko 번역은 `i18n/ko/code.json` 에서 수동 입력 (자동 번역 금지 정책).

### 5.5 색상/디자인 변경

대부분 `src/css/custom.css` 의 CSS 변수만 수정하면 사이트 전반에 반영된다. 그라데이션이 직접 들어간 곳 두 군데(`index.module.css` 의 hero 와 CTA shadow)는 같이 손볼 것.

### 5.6 favicon / 로고 교체

- favicon: `docusaurus/static/img/favicon.ico` 만 교체.
- 로고: `docusaurus/static/img/emanaul_logo.webp` 교체. width 는 `custom.css` 의 `.navbar__logo img { width: 150px }` 로 고정.

---

## 6. CI / 배포 (`.github/workflows/deploy.yml`)

- 트리거: master push (paths `docusaurus/**` 또는 `.github/workflows/deploy.yml`), workflow_dispatch
- 환경: Ubuntu, Node 20, npm ci
- 빌드 환경변수: `DOCUSAURUS_IGNORE_SSG_WARNINGS=true` (cosmetic SSG 워닝의 빌드 차단 방지)
- 업로드: `actions/upload-pages-artifact@v3` (path: `docusaurus/build`)
- 배포: `actions/deploy-pages@v4`

`docusaurus.config.ts` 의 빌드 옵션:
- `onBrokenLinks: 'throw'` (필수 — 깨지면 빌드 실패)
- `onBrokenAnchors: 'ignore'` (`<a id>` invisible anchor가 Docusaurus 검증 알고리즘에 안 잡혀서 끔. 실제 anchor scroll 은 정상)
- `onBrokenMarkdownLinks: 'warn'` / `onBrokenMarkdownImages: 'warn'`

---

## 7. 알려진 한계 / 후속 작업

1. **다국어 redirect**: 현재 en URL만 redirect. kr/jp 원본 URL → ko 페이지 redirect 는 client-redirects 플러그인의 valid-path 검증 한계 (카테고리 인덱스 인식 못 함) 로 보류. 호스팅 레벨 redirect 또는 별도 미들웨어로 후속.
2. **ko 본문 영문 잔재**: 대량 마이그레이션 페이지 일부에 영문 fallback 이 남아있다 (kr 원본 부재 페이지). 자동번역 금지 정책상 ROBOTIS 도메인 표기로 수동 채워야.
3. **broken anchor 워닝 (silent)**: `onBrokenAnchors: 'ignore'` 로 로그 정리. 시각 검증으로 실제 broken 발견 시 case-by-case.
4. **GitHub Pages 1 GB 한계**: 현재 949 MB 로 통과 가능. 향후 콘텐츠 증가시 호스팅 변경 (Cloudflare Pages 권장) 검토.
5. **DYD 시리즈 누락**: 원본에 DYNAMIXEL DRIVE 시리즈가 있지만 `source/docs/en/dxl/` 에 별도 폴더 없어 변환 누락.
6. **mega-menu thumbnail 일부 표기**: `robotos-op.webp` (의도 표기), `rh-p12-rn-ur.webp` 미준비 (현재 `rh-p12-rn.webp` 공유), `Y-series.webp` 대문자. turtlebot3 추가 완료.
7. **R+ 1.0 / 2.0 / 3.0 / Mobile App 콘텐츠 삭제됨**: Software 카테고리에서 4종 폴더(`docs/software/rplus1`, `rplus2`, `rplustask3`, `rplus_mobile`, `mobile_app`)를 제거. 본문/사이드바에서 참조하던 모든 로컬 링크 `/docs/software/rplus*`, `/docs/software/mobile_app/*` 는 외부 URL `https://emanual.robotis.com/docs/<en|kr>/software/...` 로 강등되어 있다. 향후 이 도메인이 폐쇄되면 dead link 가 되므로 `grep -r emanual.robotis.com/docs docusaurus/docs docusaurus/i18n` 으로 일괄 점검 필요.
8. **AI Worker / AI Sapiens / HX5-D20 / OMX**: 현재 본문이 "준비 중" placeholder 1페이지뿐. mega-menu 진입 시 사이드바도 1항목만 표시. 콘텐츠 작성 후 별도 작업 없이 자동으로 사이드바에 들어감 (각 폴더가 autogenerated).

---

## 8. 핵심 커밋 히스토리

작업 흐름 추적용. master log 에서 검색 가능.

| 단계 | 키워드 | 메시지 |
|---|---|---|
| 인프라 | `scaffold` | scaffold Jekyll → Docusaurus migration project |
| 디자인 | `redesign homepage` | hero / 4 cards / Pretendard |
| 자산 | `PNG → WebP` | convert 6196 PNG → WebP q=85 |
| 자산 | `JPG → WebP` | convert JPG → WebP q=85 |
| i18n | `remove ja locale` | ja 완전 삭제 (build -400 MB) |
| navbar | `mega-menu` | Platform / Systems mega-menu |
| navbar | `mega-menu sticky` | JS-managed sticky (panel stays on cursor move) |
| navbar | `mega-menu thumbnails` | 21장 썸네일 적용 + 원형 마스크 제거 |
| brand | `primary color` | #00a0df → #2160fd |
| sidebar | `hand-curated dxl sidebar` | 자동 생성 → 명시 카테고리 트리 |
| sidebar | `dxl series Overview` | Y/P/X/PRO 인덱스 라벨을 "Overview" + 맨 위 고정 |
| systems | `TurtleBot3 mega-menu + per-product sidebars` | platformSidebar 분할 14개 + TurtleBot3 카테고리 추가 |
| nav-fix | `repair X/PRO/TurtleBot3 mega-menu 404s` | doc-id 형식 href → 폴더 URL, generated-index 래퍼 |
| software | `remove R+ Task 3.0 / Mobile Apps / 2.0 / 1.0` | 4개 폴더 삭제 + 로컬 링크 외부 URL 강등 |
| software | `drop Mobile App / mini_app` | mobile_app 폴더 삭제 + Overview 사이드바 라벨 정리 |
| i18n/ko | `blank navbar title` | ko에서만 보이던 "ROBOTIS e-Manual" 텍스트 제거 |
| home | `Echosystem → Ecosystem` | Platform 카드 카피 정정 |

---

## 9. 연락처 / 책임 분담 (이관 시 갱신)

| 영역 | 담당 | 비고 |
|---|---|---|
| 콘텐츠 (mdx) | (담당자) | 페이지 추가/수정, 한국어 번역 |
| 디자인 (CSS / 컴포넌트) | (담당자) | `src/css/custom.css`, `src/pages/index.module.css` |
| 자산 최적화 | (담당자) | `scripts/` 운영 |
| CI / 배포 | (담당자) | `.github/workflows/deploy.yml`, DNS |
| 도메인 / 호스팅 | (담당자) | docs.robotis.com (GitHub Pages → 미래 Cloudflare?) |

---

## 10. 1차 완료 (이관 시점) 스냅샷

태그: `v1.0-phase1` (master 의 동일 커밋에 부착)

이관 시점에 정상 동작 확인된 항목:

- 홈 (`/`) — Hero / 4 product cards (Ecosystem 카피 적용) / Popular guides
- DYNAMIXEL 메가메뉴 — 9 시리즈 + Quick Start + Protocol, 모든 진입 URL 200 OK
- Systems 메가메뉴 — Humanoid 4 / Robot Hand 2 / Manipulator 5 / TurtleBot3 1 (총 12개 진입 URL + TurtleBot3 generated-index)
- Software 메가메뉴 → Overview 첫 화면 + 사이드바 첫 항목
- ko locale — navbar 좌측 빈 title (e-Manual 중복 제거), 사이드바 라벨 한국어 (개요 등)
- 로컬 빌드 SUCCESS, GitHub Actions 자동 배포 성공
- 빌드 산출물 사이즈: 약 1.3 GB (로컬) → dedupe 후 약 949 MB (Pages artifact)

이관 후 우선 검토 권장:

1. R+ 외부 URL 의존 (§7.7) — emanual.robotis.com 도메인 폐쇄 일정 확인
2. AI Worker / AI Sapiens / HX5-D20 / OMX placeholder 본문 (§7.8) — 콘텐츠팀 일정
3. ko 본문 영문 잔재 (§7.2) — 번역 일정
4. Cloudflare Pages 이전 검토 (§4.3)

---

> 이 문서는 마이그레이션 + UI/자산 최적화 작업의 운영 매뉴얼이다. 새 작업 패턴이 추가되면 본 문서를 함께 갱신할 것.
