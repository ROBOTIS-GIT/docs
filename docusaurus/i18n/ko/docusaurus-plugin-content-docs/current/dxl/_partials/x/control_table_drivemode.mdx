{/* 파본 있음 — 아래 파일도 함께 고쳐야 합니다.
   control_table_drivemode_x540.mdx
*/}
{/* props
   since: 값을 주면 "Drive Mode는 펌웨어 V(since)부터 지원" 문장을 덧붙인다. X430은 38.
   torqueOnFw: Torque On by Goal Update 지원 시작 펌웨어. 기본 45, X330은 46.
   timeBasedNote: false면 Time-based Profile 펌웨어 V42 안내를 숨긴다(XL330, XC330, XM335).
   invertsAll: 반전되는 항목을 Position/Velocity/Current/PWM으로 적는다(XW). 기본은 Goal/Present Position.
*/}

import Show from '@site/src/components/Show';

Drive Mode(10)는 DYNAMIXEL의 구동 방식을 설정{props.since ? <>하며, <strong>펌웨어 V{props.since}</strong>부터 지원됩니다.</> : '합니다.'}

|     Bit     |                Item                 | 설명 |
|:-----------:|:-----------------------------------:|:-----|
| Bit 7(0x80) |                  -                  | 사용하지 않으며 항상 `0`입니다. |
| Bit 6(0x40) |                  -                  | 사용하지 않으며 항상 `0`입니다. |
| Bit 5(0x20) |                  -                  | 사용하지 않으며 항상 `0`입니다. |
| Bit 4(0x10) |                  -                  | 사용하지 않으며 항상 `0`입니다. |
| Bit 3(0x08) |      Torque On by Goal Update       | **[0]** [Torque Enable(64)] 값이 `1`일 때만 명령을 수행합니다.<br />**[1]** [Torque Enable(64)] 설정값과 관계없이 명령을 수행합니다. Torque Enable(64)이 `0`인 상태에서 명령이 입력되면 Torque Enable(64)이 `1`로 전환된 뒤 명령을 수행합니다. |
| Bit 2(0x04) |        Profile Configuration        | **[0]** Velocity-based Profile: 속도 기준으로 Profile을 생성합니다.<br />**[1]** Time-based Profile: 시간 기준으로 Profile을 생성합니다.<br />※ [What is the Profile](#what-is-the-profile)을 참고하세요. |
| Bit 1(0x02) |                  -                  | 사용하지 않으며 항상 `0`입니다. |
| Bit 0(0x01) |         Normal/Reverse Mode         | **[0]** Normal Mode: CCW(양수), CW(음수)<br />**[1]** Reverse Mode: CCW(음수), CW(양수) |

<Show when={props.timeBasedNote !== false}>

:::note

**참고** : Time-based Profile은 **firmware V42**부터 사용할 수 있습니다.

:::

</Show>

:::note

**참고** : Torque On by Goal Update는 **firmware V{props.torqueOnFw ?? 45}**부터 사용할 수 있습니다.

:::

:::note

**참고** : Drive Mode(10)의 Bit 0(Normal/Reverse Mode)을 `1`로 설정하면 회전 방향이 반전됩니다.
따라서 {props.invertsAll ? <><strong>Position</strong>, <strong>Velocity</strong>, <strong>Current</strong>, <strong>PWM</strong></> : <><strong>Goal Position</strong>, <strong>Present Position</strong></>}의 방향도 반전됩니다.
이 기능은 좌우 대칭 관절을 구성할 때 유용합니다.

:::

[Torque Enable(64)]: #torque-enable64
