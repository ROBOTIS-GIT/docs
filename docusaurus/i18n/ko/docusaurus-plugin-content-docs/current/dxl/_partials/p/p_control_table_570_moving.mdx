이 값은 장치가 움직이는 중인지 나타냅니다. Present Velocity(576)의 절대값이 Moving Threshold(24)보다 크면 Moving(570)이 `1`로 설정되고, 그렇지 않으면 `0`으로 해제됩니다.

단, Goal Position(564) Instruction에 의해 Profile이 진행 중이면 Present Velocity(576)와 관계없이 이 값은 항상 `1`로 설정됩니다.
