이 값은 DYNAMIXEL이 움직이는 중인지 판단하는 데 사용됩니다.
Present Velocity(615)가 Moving Threshold(17) 값보다 크면 Moving(610)이 `1`로 설정되고, 그렇지 않으면 `0`으로 해제됩니다.
이 값은 DYNAMIXEL이 움직이는 중인지 판단합니다.

|값 범위|
| :---: |
|0 ~ 2,147,483,647|
