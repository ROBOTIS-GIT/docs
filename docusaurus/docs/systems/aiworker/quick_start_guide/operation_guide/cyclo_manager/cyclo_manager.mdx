# Cyclo Manager

**Cyclo Manager** is a web-based operation and maintenance tool for the **AI Worker, OMY, and OMX**.
It runs on the robot computer and provides a browser UI for common robot-side tasks such as
bringup, container management, log inspection, version updates, noVNC access, and manual jog control.

Instead of entering the robot container and running several commands by hand, you can open Cyclo Manager from a browser and control the system through a small set of pages.
This is useful during setup, daily operation, and software updates.

:::info
Cyclo Manager is installed on the **AI Worker Orin** and is accessed from a PC, tablet, or phone on the same network.
The web UI runs on port **3000**, and the API server runs on port **8081**.
:::

## What You Can Do

Use Cyclo Manager to:

- Bring up the AI Worker robot stack and related services from the browser.
- Start, stop, restart, and inspect Docker containers running on the robot.
- Monitor robot status, Docker status, storage usage, and version status from the Dashboard.
- Open a browser-based terminal into docker containers.
- Use noVNC to run GUI tools such as **Dynamixel Wizard 2.0** without connecting a monitor.
- Jog the mobile base from a PC or phone browser.

## How It Runs

Cyclo Manager is composed of a Python CLI, a web UI, an API server, and small agents that run inside managed containers.

- The **`cyclo-manager`** Python package provides the **`cyclo_manager`** CLI.
- The CLI creates the Docker Compose stack and registers the host-side agent.
- The **Web UI** provides browser pages for operation and monitoring.
- The **API Server** receives UI requests, controls Docker, communicates with container agents, and exposes log streams.
- The **s6 agent** inside each robot container starts and stops ROS 2 services and reports service status.
- The **host agent** handles host-level operations such as repository update workflows.

## Software Architecture

![Cyclo Manager Software Architecture](/img/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/sw_architecture.svg)

At a high level, Cyclo Manager separates user-facing controls from the robot runtime:

- **User layer**: A browser is used to operate the robot and CLI is used to install the Cyclo Manager stack.
- **Manager layer**: The Web UI and API Server provide the control surface.
- **Robot container layer**: Robot bringup services run inside containers managed by s6.
- **Host layer**: Docker, storage, serial devices, and git repositories remain on the robot host.

This layout keeps normal operation in the browser while preserving access to the underlying Docker and ROS 2 system when deeper debugging is needed.

## Typical Workflow

1. Install Cyclo Manager on the AI Worker Orin.
2. Open the Cyclo Manager web UI from a browser.
3. Check robot and container status on the Dashboard.
4. Start the required services from the System page.
5. Use logs, topic status, noVNC, terminal, and jog controls as needed.
6. Use Version Update when the robot software stack needs to be updated.

## Choose a Guide

- [Install](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/install): Install the CLI, start the Cyclo Manager stack, and open the web UI.
- [Bringup](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/bringup): Bring up the Zenoh daemon, robot stack, and Physical AI server from the System page.
- [Manage Container](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/manage_container): Start, stop, and configure containers, edit **`.bashrc`**, and run commands in a terminal.
- [Version Update](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/version_update): Update package versions from the Dashboard.
- [noVNC](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/novnc): Start the noVNC container and use Dynamixel Wizard 2.0 in the browser.
- [Jog](/docs/systems/aiworker/quick_start_guide/operation_guide/cyclo_manager/jog): Move the robot manually from the Jog page in the browser.
